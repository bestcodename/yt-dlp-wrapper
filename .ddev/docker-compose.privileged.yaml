services:
  web:
    privileged: true
    volumes:
      # Host devtmpfs: hot-plugged block devices (USB sticks) get their /dev nodes
      # created by host udev; the container's own /dev is a static snapshot from
      # container start and never sees them.
      - /dev:/dev
