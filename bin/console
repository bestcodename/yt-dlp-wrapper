#!/usr/bin/env php
<?php

declare(strict_types=1);

require dirname(__DIR__).'/vendor/autoload.php';

use App\Command\PlaylistsSyncCommand;
use App\Command\UsbSetupCommand;
use Symfony\Component\Console\Application;

$app = new Application('yt-dlp tools', '1.0.0');
$app->addCommands([
        new UsbSetupCommand(),
        new PlaylistsSyncCommand(),
]);
$app->run();
